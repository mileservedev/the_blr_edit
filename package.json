{
  "name": "the-blr-edit",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "build": "node --check server.js && node --check app.mjs && node --check lib/supabase-store.mjs && node --check public/app.js",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.0",
    "file-type": "^21.0.0",
    "helmet": "^8.1.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  },
  "main": "server.js"
}
